IF( ZLIB_INCLUDE_DIR AND ZLIB_LIBRARIES )
  SET( ZLIB_FIND_QUIETLY FALSE )
ENDIF( ZLIB_INCLUDE_DIR AND ZLIB_LIBRARIES )

if( WIN32 )
  set(ZLIB_INCLUDE_DIR $ENV{ZLIB_INCLUDE_DIR})
  #FIND_LIBRARY( ZLIB_LIBRARY NAMES "zlib" PATHS "C:/tools/zlib/lib" NO_DEFAULT_PATH)
  set(ZLIB_LIBRARY "$ENV{ZLIB_LIBRARIES}")
  set(ZLIB_LIBRARIES "$ENV{ZLIB_LIBRARIES}")
  find_package( zlib REQUIRED )
endif( WIN32 )

if(UNIX)
  if(APPLE)
    # find_package( zlib REQUIRED )
    INCLUDE( FindPackageHandleStandardArgs )
    FIND_PACKAGE_HANDLE_STANDARD_ARGS( zlib DEFAULT_MSG ZLIB_LIBRARIES ZLIB_INCLUDE_DIR )
  else(APPLE)
    #set(ZLIB_INCLUDE_DIR "/usr/include")
    #set(ZLIB_INCLUDE_DIRS "/usr/include")
    #set(ZLIB_LIBRARIES "/usr/lib64")
    INCLUDE( FindPackageHandleStandardArgs )
    FIND_PACKAGE_HANDLE_STANDARD_ARGS( zlib DEFAULT_MSG ZLIB_LIBRARIES ZLIB_INCLUDE_DIR )
  endif(APPLE)
endif(UNIX)  



MARK_AS_ADVANCED( ZLIB_INCLUDE_DIR ZLIB_LIBRARIES )
